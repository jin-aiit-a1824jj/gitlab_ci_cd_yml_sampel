stages:
  - build
  - test

build wesite:
  stage: build
  image: node:14-alpine
  script:
    - ls -al
    - cd ./session2/static-website/
    - npm install
    - npm install -g gatsby-cli
    - gatsby build
  artifacts:
    paths:
      - ./session2/static-website

test artifacts:
  stage: test
  script:
    - grep "Gatsby" ./session2/static-website/index.html
    - grep "xxxxxx" ./session2/static-website/index.html